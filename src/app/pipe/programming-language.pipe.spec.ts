import { ProgrammingLanguagePipe } from './programming-language.pipe';

describe('ProgrammingLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new ProgrammingLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
