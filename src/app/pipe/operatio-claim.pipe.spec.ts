import { OperatioClaimPipe } from './operatio-claim.pipe';

describe('OperatioClaimPipe', () => {
  it('create an instance', () => {
    const pipe = new OperatioClaimPipe();
    expect(pipe).toBeTruthy();
  });
});
