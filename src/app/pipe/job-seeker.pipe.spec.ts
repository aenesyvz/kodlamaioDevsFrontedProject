import { JobSeekerPipe } from './job-seeker.pipe';

describe('JobSeekerPipe', () => {
  it('create an instance', () => {
    const pipe = new JobSeekerPipe();
    expect(pipe).toBeTruthy();
  });
});
