import { ProgrammingTechnologyPipe } from './programming-technology.pipe';

describe('ProgrammingTechnologyPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgrammingTechnologyPipe();
    expect(pipe).toBeTruthy();
  });
});
